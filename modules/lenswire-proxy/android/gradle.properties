# Used when this directory is opened as a standalone Gradle root (Cursor Kotlin LS).
# Harmless when the module is included from the app android/ project.
android.useAndroidX=true
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m
