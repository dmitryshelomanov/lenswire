/** Canonical site origin (includes GitHub Pages project path). */
export const SITE_URL = 'https://dmitryshelomanov.github.io/lenswire';

export const SITE_TITLE = 'Lenswire — Local HTTP(S) inspector';

export const SITE_DESCRIPTION =
  'Capture, decrypt, and debug HTTP(S) traffic on iOS and Android — VPN MITM, overrides, and HAR export. On-device.';
